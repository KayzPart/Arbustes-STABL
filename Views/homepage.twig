{% extends "base.twig" %}

{% block body %}
{{ include("Templates/header.twig") }}
<div class="container">
    <h2>Bonjour "Alexandre"</h2>
    <section class="choix-tables">
        <form action="" method="GET">
            {% for i in 1..10 %}
                <label>
                    <input type="radio" name="param1" value="{{i}}">
                    <img src="./public/assets/ressources/click-{{i}}.png" alt="table-{{i}}">
                </label>
            {% endfor %}
        </form>
    </section>
</div>
{{ include("Templates/footer.twig") }}
{% endblock %}